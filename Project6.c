#include <stdio.h>

int main(){

    //arithemic operators

    // + (addition)
    // - (subtraction)
    // * (multiplication)
    // / (division)
    // % (modulus)
    // ++ increment
    // -- decrement

    int x = 5;
    int y = 2;

    //int z = x + y;
    //int z = x - y;
    //int z = x * y;
    //int z = x / (float) y;

    
    x++;
    y--;

    printf("%d", y);

    return 0;
}